# {{projectName}}

{{description}}

## Setup

```bash
# Install dependencies
{{#if (eq packaging "uv")}}uv sync{{/if}}
{{#if (eq packaging "poetry")}}poetry install{{/if}}
{{#if (eq packaging "pipenv")}}pipenv install{{/if}}
{{#if (eq packaging "pip")}}pip install -r requirements.txt{{/if}}

# Run the application
{{#if (eq packaging "uv")}}uv run uvicorn main:app --reload{{/if}}
{{#if (eq packaging "poetry")}}poetry run uvicorn main:app --reload{{/if}}
{{#if (eq packaging "pipenv")}}pipenv run uvicorn main:app --reload{{/if}}
{{#if (eq packaging "pip")}}uvicorn main:app --reload{{/if}}
```

## API Documentation

Once running, visit:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc
